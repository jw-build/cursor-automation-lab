# Cursor Practice Folder

This is my cursor practice folder.

**Today's goal:** Learn cursor basics

**Next:** Create a python script and run it

### Run the script

From the project root:

```bash
python scripts/hello.py
```

### Run the CLI

From the project root:

```bash
python automation/cli.py <command>
```

Commands: `hello`, `time`, `sysinfo`. Use `--help` for details.
